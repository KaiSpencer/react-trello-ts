{
	"name": "react-trello-ts-root",
	"scripts": {
		"build": "turbo run build",
		"test:unit": "turbo run test:unit",
		"test:integration": "turbo run test:integration",
		"lint": "turbo run lint",
		"format": "biome format .",
		"dev": "turbo run dev",
		"release": "turbo run build --filter=react-trello-ts && changeset publish"
	},
	"devDependencies": {
		"@biomejs/biome": "^1.4.1",
		"turbo": "^1.6.3",
		"typescript": "^4.9.3"
	},
	"dependencies": {
		"@changesets/cli": "^2.26.0"
	}
}
